<!DOCTYPE html>
<html>
    <head>
        <title><%= title %></title>
        <link href="/stylesheets/styles.css" rel="stylesheet" />
    </head>
    <body>
        <nav class="navigation">
            <a href="/dmindex" class="button">Dark Mode</a>
        </nav>
        <h1 class="header">Student Grade Table</h1>
        <table class="table" id="student-table">
            <thead>
                <tr>
                    <th><a href="/?sort=name&dir=<%= dir * -1 %>" id="thead-name">Name</a></th>
                    <th><a href="/?sort=grade&dir=<%= dir * -1 %>" id="thead-grade">Grade</a></th>
                    <th>Options</th>
                </tr>
            </thead>
            <tbody>
                <% users.forEach( user => { %>
                    <tr>
                        <% if(user._id == id) { %> 
                            <td><input id="name-input"
                                form="updateForm"
                                type="text" size="8"
                                name="name"
                                value=<%= name %>
                                placeholder="Name" required /></td>
                            <td><input id="grade-input"
                                form="updateForm"
                                type="tel" maxlength="3"
                                value=<%= grade %>
                                name="grade"
                                placeholder="Grade" required /></td>
                            <td>
                                <a href="/edit/<%= user._id %>/?sort=<%= sort %>&dir=<%= dir %>&name=<%= name %>&grade=<%= grade %>" class="more-btn">Reset</a>
                                <button form="updateForm" type="submit" class="more-btn">Save</button>
                                <a href="/?sort=<%= sort %>&dir=<%= dir %>" class="more-btn">Cancel</a>
                            </td>
                        <% } else { %>
                            <td class="name-col"><span
                                data-field="name"><%= user.name %></span></td>
                            <td class="grade-col"><span
                                data-field="grade"><%= user.grade %></span></td>
                            <td>
                                <div class="option">
                                    <button class="option-btn">...</button>
                                    <div class="option-list">
                                    <a href="/edit/<%= user._id %>/?sort=<%= sort %>&dir=<%= dir %>&name=<%= user.name %>&grade=<%= user.grade %>" class="edit-btn">Edit</a>
                                    <a href="/delete/<%= user._id %>" class="delete-btn">Delete</a>
                                    </div>
                                </div>
                            </td>
                        <% } %>  
                    </tr>
                <% }); %>
            </tbody>
            <tfoot>
                <tr>
                    <form id="add-form">
                        <td><input id="name-input"
                                form="studentForm"
                                type="text" size="8"
                                name="name"
                                placeholder="Name" required /></td>
                        <td><input id="grade-input"
                                form="studentForm"
                                type="tel" maxlength="3"
                                name="grade"
                                placeholder="Grade" required /></td>
                        <td>
                            <div class="option">
                                <button class="option-btn"
                                    form="studentForm"
                                    id="btn-add"
                                    type="submit">Add</button>
                            </div>
                        </td>
                    </form>
                </tr>
            </tfoot>
        </table>
        <form id="studentForm" method="POST" action="/"></form>
        <form id="updateForm" method="POST" action="/save/<%= id %>/?sort=<%= sort %>&dir=<%= dir %>"></form>
    </body>
</html>
